# Requirements for GT23 Film Workflow GUI
# EN: Python 3.11 recommended for best compatibility
# CN: 推荐使用 Python 3.11 以获得最佳兼容性

# GUI Framework / GUI框架
PySide6==6.10.1

# Core dependencies (shared with CLI) / 核心依赖（与CLI共享）
Pillow>=12.0.0
ExifRead>=3.5.0
fonttools>=4.60.0

# Optional but recommended / 可选但推荐
numpy>=2.0.0
aggdraw>=1.3.0
